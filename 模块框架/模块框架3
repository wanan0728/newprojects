# 3. 审计业务模块说明
#
# 这个模块（audit）实现了完整的审计日志功能，用于记录用户的关键操作。
# 审计日志是追踪谁、在什么时间、做了什么操作的重要工具，
# 对于安全合规、问题排查、行为分析都至关重要。
# 模块采用分层设计，职责清晰，便于维护和扩展。

app/
└── modules/                               # 业务模块集合
    └── audit/                             # 审计日志模块
        ├── context.py                  (3.1)  # 审计上下文管理，基于ContextVar存储当前请求的审计事件
        ├── models.py                    (3.2)  # 审计数据库模型，定义表结构和字段
        ├── service.py                    (3.3)  # 审计核心服务，提供record函数记录审计事件
        ├── middleware.py                 (3.4)  # 审计中间件，在请求结束时自动将事件写入数据库
        └── hook.py                       (3.5)  # 审计钩子，延迟加载实现，避免循环导入

# 各模块职责说明：
#
# 3.1 context.py:     使用ContextVar在请求上下文中存储审计事件列表
#                    提供init、add、pop、clear等操作审计事件的方法
#
# 3.2 models.py:      定义AuditEvent数据库模型，包含审计需要的所有字段
#                    如：请求ID、用户ID、操作名称、资源类型、状态、IP等
#
# 3.3 service.py:     核心服务，提供record函数供业务代码调用
#                    自动收集请求上下文信息，格式化后添加到审计上下文中
#
# 3.4 middleware.py:  FastAPI中间件，在请求结束时自动将审计事件保存到数据库
#                    如果没有审计事件但有错误，自动创建错误审计记录
#
# 3.5 hook.py:        钩子模块，延迟加载实际实现，解决循环导入问题
#                    提供与service/middleware相同的接口，但内部动态导入

# 模块工作流程：
#
# 1. 请求开始 → 中间件调用 init_audit() 初始化审计上下文（空列表）
#         ↓
# 2. 业务代码 → 调用 service.record() 记录审计事件（可多次调用）
#              ↓
#        事件被添加到上下文列表中 (context.py)
#         ↓
# 3. 请求结束 → 中间件调用 flush_audit()
#              ↓
#       从上下文取出所有事件 (pop_audit_events)
#              ↓
#       如果没有事件但有错误，创建默认错误事件
#              ↓
#       将事件保存到数据库 (models.py)
#              ↓
# 4. 清理上下文 → 清除当前请求的审计数据，不影响下一个请求

# 为什么要这么设计？
#
# 1. 上下文存储 (context.py)：使用ContextVar可以在异步请求中隔离数据
#                            每个请求有自己的审计事件列表，互不干扰
#
# 2. 延迟保存 (middleware.py)：在请求结束时统一保存，减少数据库连接次数
#                            如果中途出错，也能保证审计日志被记录
#
# 3. 钩子模式 (hook.py)：解决循环导入，让其他模块可以直接from audit import record
#                       而不需要依赖service模块的具体实现
#
# 4. 自动错误审计：即使业务代码忘记记录审计，出现错误时也会自动记录
#                 确保重要操作都有迹可循